/**
 * Averages
 */
/**
 * Initialise the variable group one to the value of 3
 * Initialise the variable group two to the value of 6
 * Initialise the varaiable group threee to the value of 12
 * 
 * Initialise the variable average groups to the average of the 3 valuse above
 * Write a single line output as to what will be displayed.
 */
var groupOne = 3;
var groupTwo = 6;
var groupThree = 12;

var averageGroups = (groupOne + groupTwo + groupThree) / 3;
console.log(averageGroups); // 7

/**
 * Averages
 */
/**
 * Initialise the variable box one to the value of 10
 * Initialise the variable box two to the value of 14
 * Initialise the varaiable box threee to the value of 21
 * 
 * Initialise the variable box average to the average of the three boxes. 
 * Write a single line output as to what will be displayed.
 */
var boxOne = 10;
var boxTwo = 14;
var boxThree = 21;

var boxAverage = (boxOne + boxTwo + boxThree) / 3;
console.log(boxAverage); //15